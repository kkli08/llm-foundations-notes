# 每日自动整理规则

## 运行时间

采用“当天主整理 + 次日补漏”两个批次：

```text
每天 18:25：整理当天截至此时已经记录、尚未整理的内容
每天 09:25：补查前一天 18:25～23:59 新增且尚未整理的内容
时区：Asia/Shanghai
```

次日补漏仍归入内容实际发生的前一天，而不是归到运行补漏任务的当天。

例如：

```text
2026-07-24 21:10 学到的新知识
→ 2026-07-25 09:25 被补漏任务发现
→ 继续写入 2026-07-24 的正式笔记
→ 整理记录注明“2026-07-25 09:25 次日补录”
```

## 输入来源

自动任务只整理仓库中可以确认的材料：

```text
inbox/目标日期.md
INDEX.md
reviews/QUICK_REVIEW.md
最近三篇正式笔记
目标日期相关的项目内 Markdown 学习记录
```

它不会假设自己能够读取所有 Codex 对话，也不会在没有材料时编造“今日学习内容”。

## 当天 18:25 主整理步骤

1. 进入知识库目录；
2. 检查 Git 状态，保护用户未提交的修改；
3. 从远端执行安全的 fast-forward 同步；
4. 阅读总索引、快速复习页和最近三篇正式笔记；
5. 查找当天 Inbox 中所有“未整理”条目；
6. 如果没有尚未整理的实质内容，停止且不创建提交；
7. 如果有内容，按模板创建或更新当天正式笔记；
8. 保留用户的提问顺序和容易混淆的点；
9. 对事实性内容做一致性检查，不确定内容明确标记；
10. 避免复制整段旧笔记，优先建立相对链接；
11. 在正式笔记“整理记录”中记录当天 18:25 主整理；
12. 把成功写入正式笔记的 Inbox 条目标记为“已整理”，并写入正式笔记链接；
13. 更新 `INDEX.md`；
14. 更新 `reviews/QUICK_REVIEW.md` 的复习队列；
15. 检查 Markdown 标题、代码块、相对链接和敏感信息；
16. 只暂存本知识库内本次相关文件；
17. 使用日期明确的提交信息提交；
18. 推送到默认分支；
19. 汇报新增笔记、更新索引和推送结果。

## 次日 09:25 补漏步骤

1. 目标日期固定为北京时间的前一个自然日；
2. 只检查目标日期 Inbox 中记录时间位于 18:25～23:59、整理状态仍为“未整理”的条目；
3. 同时检查第二天 09:25 前补写、但明确标记“内容归属日期”为前一天的条目；
4. 先对照前一天正式笔记，确认内容尚未写入，避免仅因状态标记遗漏而重复添加；
5. 将缺失内容整合进前一天的正式笔记，而不是创建第二天的笔记；
6. 在前一天正式笔记“整理记录”中记录实际补录时间；
7. 将对应 Inbox 条目标记为“已补录”，同时记录归档日期、补录时间和正式笔记链接；
8. 更新索引或复习队列时仍使用前一天作为知识发生日期；
9. 如果没有符合条件的未整理内容，不修改文件、不创建提交；
10. 提交信息使用 `notes: 补录 YYYY-MM-DD 晚间学习内容`，其中日期为内容归属日期；
11. 安全推送到默认分支并汇报结果。

## 如何判定“未添加”

优先使用 Inbox 条目中的整理状态：

```text
整理状态：未整理
整理状态：已整理
整理状态：已补录
```

自动任务还必须对照正式笔记做一次语义检查：

- 状态为“未整理”但正式笔记已经包含：不重复添加，只修正状态；
- 状态为“已整理”但正式笔记实际缺失：报告异常，不静默假设成功；
- 没有状态的旧格式内容：先与正式笔记比对，只补充确实缺失的部分，再补上状态；
- 同一知识在多个 Inbox 条目重复出现：合并成一处连贯解释，不重复堆叠。

## 安全边界

- 不执行 `git reset --hard`；
- 不覆盖无法确认来源的用户修改；
- 工作区存在冲突或不相关修改时停止推送并报告；
- 不提交密码、访问令牌、Cookie、私钥或敏感业务数据；
- 不为了“每天必须有提交”而创建空笔记；
- 推送失败时保留本地提交并报告，不反复重写历史；
- 不使用强制推送；
- 不自动删除原始 Inbox；
- 不修改知识库目录之外的项目文件。

## 每日提交信息

推荐格式：

```text
notes: 整理 2026-07-24 学习笔记
```

次日补漏：

```text
notes: 补录 2026-07-24 晚间学习内容
```

仅更新复习状态时：

```text
review: 更新 2026-07-24 复习记录
```
