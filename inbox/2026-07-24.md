# 2026-07-24 原始学习记录

## 本次整理｜从指定 Codex Session 提取基础理论

> 记录时间：2026-07-24（本次手动整理，未记录具体时分）
>
> 内容归属日期：2026-07-24
>
> 整理状态：已整理
>
> 来源 Session：`019f8e1a-1ff1-7002-aa46-85ffd51a11cc`
>
> 正式笔记：[从预训练到 RL 后训练：MTP 与状态管理基础](../notes/2026/07/训练与RL后训练基础_20260724.md)

### 提取范围

本次只提取可跨项目复用的大模型基础理论：

- 预训练、SFT、RL 后训练的目标与关系；
- Actor、Rollout、Reward、Reference、Advantage 的作用；
- 训练引擎、推理引擎与权重同步；
- MTP 的训练侧与推理侧含义；
- Draft、Verify、Accept/Reject 与状态回滚；
- KV Cache、递归状态、混合 Attention 与 Prefix Caching 的关系；
- Checkpoint 和常见并行策略的最低认知。

### 明确排除

- 工作仓库名称、路径、分支与代码位置；
- 具体工作任务、优先级、排期和汇报安排；
- 内部集群、权限、监控与调试环境信息；
- 具体模型适配结论及尚未验证的项目事实；
- 会议中的个人信息和内部沟通内容。

### 本次新增理解

- 预训练、SFT 与 RL 后训练不是三种完全不同的模型架构，而是同一模型在不同数据、目标和训练闭环下的不同阶段。
- Reward 不是一段新的“标准答案文本”，而是对生成结果的评价信号；Advantage 再把这个评价变成“这次行为相对基准好多少”的更新方向。
- Actor 与 Rollout Engine 可以持有同一套模型能力，但职责不同：前者负责反向传播和更新参数，后者负责高吞吐生成；参数更新后必须同步权重。
- MTP 在训练侧是“学习预测更远的后续 token”，在推理侧通常服务于“先草拟多个 token，再由主模型验证”，两侧相关但不能混为同一个步骤。
- 推测解码不仅涉及 token 是否被接受，还涉及 KV Cache 或其他模型状态如何提交、丢弃或回滚。
- 混合 Attention 模型可能同时维护普通 KV Cache 和其他递归状态；所有状态必须对应同一个 token 边界。
