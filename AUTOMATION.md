# 每日自动整理规则

## 运行时间

默认计划：

```text
周一至周五 17:45
时区：Asia/Shanghai
```

## 输入来源

自动任务只整理仓库中可以确认的材料：

```text
inbox/当天日期.md
INDEX.md
reviews/QUICK_REVIEW.md
最近三篇正式笔记
当天相关的项目内 Markdown 学习记录
```

它不会假设自己能够读取所有 Codex 对话，也不会在没有材料时编造“今日学习内容”。

## 自动任务步骤

1. 进入知识库目录；
2. 检查 Git 状态，保护用户未提交的修改；
3. 从远端执行安全的 fast-forward 同步；
4. 阅读总索引、快速复习页和最近三篇正式笔记；
5. 查找当天 Inbox；
6. 如果没有实质内容，停止且不创建提交；
7. 如果有内容，按模板整理正式笔记；
8. 保留用户的提问顺序和容易混淆的点；
9. 对事实性内容做一致性检查，不确定内容明确标记；
10. 避免复制整段旧笔记，优先建立相对链接；
11. 更新 `INDEX.md`；
12. 更新 `reviews/QUICK_REVIEW.md` 的复习队列；
13. 检查 Markdown 标题、代码块、相对链接和敏感信息；
14. 只暂存本知识库内本次相关文件；
15. 使用日期明确的提交信息提交；
16. 推送到默认分支；
17. 汇报新增笔记、更新索引和推送结果。

## 安全边界

- 不执行 `git reset --hard`；
- 不覆盖无法确认来源的用户修改；
- 工作区存在冲突或不相关修改时停止推送并报告；
- 不提交密码、访问令牌、Cookie、私钥或敏感业务数据；
- 不为了“每天必须有提交”而创建空笔记；
- 推送失败时保留本地提交并报告，不反复重写历史；
- 不使用强制推送；
- 不自动删除原始 Inbox；
- 不修改知识库目录之外的项目文件。

## 每日提交信息

推荐格式：

```text
notes: 整理 2026-07-24 学习笔记
```

仅更新复习状态时：

```text
review: 更新 2026-07-24 复习记录
```
